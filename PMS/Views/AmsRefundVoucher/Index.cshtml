@model List<AccRefundVoucher>
@{
    ViewData["Title"] = "Refund vouchers";
}
@if (TempData["Success"] != null)
{
    <div class="alert alert-success py-2">@TempData["Success"]</div>
}
<div class="d-flex justify-content-between align-items-center mb-3 flex-wrap gap-2">
    <partial name="_AmsExportCsv" model="@("RefundVouchers")" />

    <p class="mb-0 text-muted small">Refund vouchers: <code>acc.RefundVoucher</code>. Optional PMS refund id.</p>
    @if (ViewBag.CanCreate == true)
    {
        <a class="btn btn-primary btn-sm" asp-action="Create"><i class="fas fa-plus me-1"></i>New voucher</a>
    }
</div>
<div class="table-responsive">
    <table class="table table-sm table-hover align-middle">
        <thead class="table-light">
            <tr>
                <th>No.</th>
                <th>Date</th>
                <th>Customer</th>
                <th class="text-end">Gross</th>
                <th class="text-end">Net</th>
                <th>Mode</th>
                <th>Status</th>
                <th>PMS refund</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var v in Model)
            {
                <tr>
                    <td class="text-nowrap">@v.VoucherNo</td>
                    <td>@v.VoucherDate.ToString("yyyy-MM-dd")</td>
                    <td>@v.CustomerID</td>
                    <td class="text-end">@v.GrossRefundAmount.ToString("N2")</td>
                    <td class="text-end">@v.NetRefundAmount.ToString("N2")</td>
                    <td>@v.PaymentMode</td>
                    <td>@v.Status</td>
                    <td class="small">@(v.PMSRefundID ?? "—")</td>
                    <td>
                        @if (ViewBag.CanEdit == true)
                        {
                            <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@v.RefundVoucherID">Edit</a>
                        }
                        @if (ViewBag.CanDelete == true)
                        {
                            ViewData["AmsDeleteEntity"] = "refundvoucher";
                            ViewData["AmsDeleteId"] = v.RefundVoucherID;
                            <partial name="_AmsDeleteButton" />
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
@if (!Model.Any())
{
    <p class="text-muted">No refund vouchers yet.</p>
}
