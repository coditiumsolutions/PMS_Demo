@model AccFiscalYear
@{
    ViewData["Title"] = "Edit fiscal year";
}
<form asp-action="EditYear" asp-route-id="@Model.FiscalYearID" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="FiscalYearID" />
    <div class="row g-3">
        <div class="col-md-6">
            <label asp-for="YearName" class="form-label"></label>
            <input asp-for="YearName" class="form-control" maxlength="50" required />
        </div>
        <div class="col-md-3">
            <label asp-for="StartDate" class="form-label"></label>
            <input asp-for="StartDate" class="form-control" type="date" required />
        </div>
        <div class="col-md-3">
            <label asp-for="EndDate" class="form-label"></label>
            <input asp-for="EndDate" class="form-control" type="date" required />
        </div>
        <div class="col-md-4">
            <label asp-for="Status" class="form-label"></label>
            <select asp-for="Status" class="form-select">
                <option value="Open">Open</option>
                <option value="Closed">Closed</option>
                <option value="Locked">Locked</option>
            </select>
        </div>
    </div>
    <div class="mt-3">
        <button type="submit" class="btn btn-primary">Save</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
    </div>
</form>
